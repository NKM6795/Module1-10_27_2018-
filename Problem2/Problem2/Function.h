#pragma once


#include <iostream>
#include <vector>
#include <math.h>
#include <string>


using namespace std;


template<typename T>
vector<int> function(T value);






template<typename T>
vector<int> function(T value)
{
	return vector<int>({ 4, 0, 71 });
}
